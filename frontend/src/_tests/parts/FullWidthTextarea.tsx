import { styled } from "@mui/system"
import { TextareaAutosize } from "@mui/base"

export const FullWidthTextarea = styled(TextareaAutosize)({
  width: "100%", // Make the textarea take full width of the cell
})
